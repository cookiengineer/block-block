
# TODO

- Evaluate whether `touch-action: none` is used in the wild
- Evaluate whether `visibility: hidden` or `opacity: 0` is used in the wild
- Evaluate whether `display: none` is used in the wild
- Evaluate whether `transform` and `z-index` should be blocked
- Evaluate whether HTML `inert` attribute should be blocked

